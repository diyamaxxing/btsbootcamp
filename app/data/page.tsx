// Raw JSON viewer (#9) — not yet built. Ported as a stub, matching
// pages/data.html's current unimplemented state: intentionally unstyled,
// deliberately outside the (site) route group (no Nav, no auth, no
// analytics), not linked from the main nav.
export default function DataPage() {
  return (
    <>{/* raw render of videos.json, users.json, progress (localStorage, not a file), comments.json */}</>
  );
}
