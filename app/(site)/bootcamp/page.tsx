// Guided new-ARMY path (#6) — not yet built. Per ARCHITECTURE_DECISIONS.md's
// framework-migration entry, this is deliberately left a stub: the whole
// point of migrating now was to build #6 on the Next.js/component base
// rather than on more hand-rolled HTML, so implementing it is separate,
// larger-scoped follow-up work, not part of this migration pass.
export default function BootcampPage() {
  return (
    <>
      {/* chapter list: Ch1–Ch6, with completion state */}
      {/* current step: video card + context description */}
      {/* progress bar per chapter */}
    </>
  );
}
