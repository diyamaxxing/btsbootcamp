// Collection page (#3) — not yet built. Ported as a stub, matching
// pages/collection.html's current unimplemented state.
export default function CollectionPage() {
  return (
    <>
      {/* collection header: poster, title, description */}
      {/* sortable video list: watched/unwatched per entry (depends on #8 progress tracking) */}
    </>
  );
}
